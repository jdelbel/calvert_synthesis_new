---
title: "R Notebook"
output: html_notebook
---

```{r}
library(tidyverse)
library(here)
library(patchwork)
library(readxl)
library(gsw)
library(zoo)
library(broom)
library(RColorBrewer)
```

```{r}
#Upload the full 5m chemtax timeseries - processing in same way as manuscript
chem <- read_csv(here("files", "chemtax_single_2025-12-01.csv"))

vert <- read_csv(here("outputs", "vertical_metrics.csv"))

#Corrected CTD fluorescence data
ctd_fl <- read_csv(here("outputs",
                        "flu_qc_2025-11-25.csv"))
```

```{r}
#Working with chemtax data to make it easy to plot

#Making the date column a date format
chem <- chem %>% 
  mutate(date = lubridate::mdy(Date)) %>% 
  select(date,
         station = Station,
         depth,
         cyan = Cyanobacteria,
         hapt = Hapto, 
         GA = `Prasinophytes-3`,
         cryp = Cryptophytes,
         dino = `Dinoflagellates-1`,
         dict = Dictyo,
         raph = Raphido,
         diat = `Diatoms-1`
         )

#Pivoting to long/tidy for plotting in ggplot
chem_c_long <- chem %>% 
  pivot_longer(c(cyan:diat), names_to = "group", values_to = "contribution")

#Calculating TChla for each sample
chem_c_tchla <- chem_c_long %>% 
  group_by(date, station, depth, group) %>% 
  summarise(contribution = mean(contribution)) %>% 
  ungroup() %>% 
  group_by(date, station, depth) %>% 
  mutate(tchla = sum(contribution)) %>% 
  ungroup() %>% 
  mutate(month = lubridate::month(date),
         year = lubridate::year(date),
    month_surv = case_when(date == "2020-11-27" ~ 12,
                                date == "2022-07-28" ~ 8,
                                date == "2020-04-30" ~ 5,
                                TRUE ~ as.numeric (month))) %>% 
  mutate(month_mid = 15) %>% 
  unite(month_date, c("year", "month_surv", "month_mid"), sep = "-") %>% 
  mutate(month_date = lubridate::ymd(month_date))

#Good to move forward
```

```{r}
#Setting plotting order phytoplankton groups

#Order phytoplankton groups roughly from smallest to largest - create order list
order_chem <- c("cyan",
                "hapt",
                "GA",
                "cryp",
                "dino",
                "dict",
                "raph",
                "diat")

#Chemtax full data - Specify order of phyto groups for figures
chem_c_tchla <- arrange(mutate(chem_c_tchla,
                                group = factor(group,
                                levels = order_chem)))
```

```{r}
#Set Color palette for plotting. Currently based on QU39 manuscript. Brewer has some good ones for R style.
# Alternative version with more distinct colors (no similar reds)
npg_distinct_colors <- c(
  "#E64B35",  # Red
  "#4DBBD5",  # Light blue  
  "#00A087",  # Teal
  "#3C5488",  # Dark blue
  "#F39B7F",  # Orange
  "#8491B4",  # Purple
  "#91D1C2",  # Light teal
  "#FFC107",  # Golden yellow (replacing second red)
  "#7E6148",  # Brown
  "#B09C85"   # Light brown
)

scale_fill_npg_distinct <- function(...) {
  ggplot2::scale_fill_manual(values = npg_distinct_colors, ...)
}

scale_color_npg_distinct <- function(...) {
  ggplot2::scale_color_manual(values = npg_distinct_colors, ...)
}
```

```{r}
# Define cutoff dates
cutoff_2020 <- as.Date("2020-02-11")
cutoff_2024 <- as.Date("2024-01-01")

# Split data into three periods
data_area_early <- chem_c_tchla %>% filter(date < cutoff_2020)
data_bar_2020 <- chem_c_tchla %>% filter(date == cutoff_2020)
data_area_mid <- chem_c_tchla %>% filter(date > cutoff_2020 & date < cutoff_2024)
data_bars_late <- chem_c_tchla %>% filter(date >= cutoff_2024)

ggplot() +
  # Area plot for pre-2020-02-11
  geom_area(data = data_area_early,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  # Single bar for 2020-02-11
  geom_bar(data = data_bar_2020,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  # Area plot for mid period
  geom_area(data = data_area_mid,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  # Bar plot for 2024-2025
  geom_bar(data = data_bars_late,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  facet_grid(station ~ .) +
  scale_fill_npg_distinct() +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")")),
    title = "Phytoplankton community composition"
  ) +
  theme_bw() +
  theme(
    legend.position = "right",
    legend.key.height = unit(0.6, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_blank(),
    strip.background = element_rect(fill = "grey90"),
    strip.text = element_text(face = "bold"),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    text = element_text(size = 28, color = "black")
  )

ggsave(here("figures", "phytoplankton_community_composition.png"), 
       width = 16, height = 8, dpi = 300)
```

```{r}
# Add TChla as a group in long format
chem_c_tchla_with_total <- chem_c_tchla %>%
  bind_rows(
    chem_c_tchla %>%
      distinct(date, station, depth, tchla) %>%
      mutate(group = "TChla",
             contribution = tchla)
  )

# Find dates where both stations were sampled
shared_dates <- chem_c_tchla_with_total %>%
  filter(station %in% c("KC10", "FZH01")) %>%
  group_by(date) %>%
  filter(n_distinct(station) == 2) %>%
  ungroup() %>%
  pull(date) %>%
  unique()

# Prepare data for comparison - wide format
compare_data <- chem_c_tchla_with_total %>%
  filter(date %in% shared_dates, station %in% c("KC10", "FZH01")) %>%
  group_by(date, station, group) %>%
  summarise(
    contribution = sum(contribution, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  pivot_wider(
    names_from = station,
    values_from = contribution,
    names_sep = "_"
  )

# Calculate relative contribution (excluding TChla)
compare_data_rel <- compare_data %>%
  filter(group != "TChla") %>%
  group_by(date) %>%
  mutate(
    rel_FZH01 = FZH01 / sum(FZH01, na.rm = TRUE),
    rel_KC10 = KC10 / sum(KC10, na.rm = TRUE)
  ) %>%
  ungroup()

# Function to get regression stats
get_stats <- function(x, y) {
  model <- lm(y ~ x)
  r2 <- summary(model)$r.squared
  slope <- coef(model)[2]
  intercept <- coef(model)[1]
  p_val <- summary(model)$coefficients[2, 4]
  n <- sum(!is.na(x) & !is.na(y))
  sprintf("y = %.2fx + %.2f\nR² = %.3f, p = %.4f\nn = %d", slope, intercept, r2, p_val, n)
}

# Stats for absolute
stats_abs <- compare_data %>%
  group_by(group) %>%
  summarise(
    label = get_stats(FZH01, KC10),
    .groups = "drop"
  )

# Stats for relative
stats_rel <- compare_data_rel %>%
  group_by(group) %>%
  summarise(
    label = get_stats(rel_FZH01, rel_KC10),
    .groups = "drop"
  )

# Panel 1: Absolute contribution (includes TChla)
p1 <- compare_data %>%
  ggplot(aes(x = FZH01, y = KC10, color = group)) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed", color = "grey40") +
  geom_smooth(method = "lm", se = TRUE, alpha = 0.2, linewidth = 0.8) +
  geom_point(size = 3, alpha = 0.7) +
  geom_text(data = stats_abs,
            aes(x = Inf, y = -Inf, label = label),
            hjust = 1.05, vjust = -0.1,
            size = 4, fontface = "italic",
            show.legend = FALSE) +
  facet_wrap(~ group, scales = "free") +
  scale_color_npg_distinct() +
  labs(
    x = expression(paste("FZH01 (mg ", m^{-3}, ")")),
    y = expression(paste("KC10 (mg ", m^{-3}, ")")),
    title = "Absolute contribution by group"
  ) +
  theme_bw() +
  theme(
    legend.position = "none",
    strip.background = element_rect(fill = "grey90"),
    strip.text = element_text(face = "bold"),
    panel.grid.minor = element_blank(),
    text = element_text(size = 28, color = "black")
  )

# Panel 2: Relative contribution (excludes TChla)
p2 <- compare_data_rel %>%
  ggplot(aes(x = rel_FZH01, y = rel_KC10, color = group)) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed", color = "grey40") +
  geom_smooth(method = "lm", se = TRUE, alpha = 0.2, linewidth = 0.8) +
  geom_point(size = 3, alpha = 0.7) +
  geom_text(data = stats_rel,
            aes(x = Inf, y = -Inf, label = label),
            hjust = 1.05, vjust = -0.1,
            size = 4, fontface = "italic",
            show.legend = FALSE) +
  facet_wrap(~ group, scales = "free") +
  scale_color_npg_distinct() +
  scale_x_continuous(labels = scales::percent) +
  scale_y_continuous(labels = scales::percent) +
  labs(
    x = "FZH01 relative contribution",
    y = "KC10 relative contribution",
    title = "Relative contribution by group"
  ) +
  theme_bw() +
  theme(
    legend.position = "none",
    strip.background = element_rect(fill = "grey90"),
    strip.text = element_text(face = "bold"),
    panel.grid.minor = element_blank(),
    text = element_text(size = 28, color = "black")
  )

# Combine
p1 / p2

ggsave(here("figures", "station_comparison_by_group.png"), 
       width = 14, height = 18, dpi = 300)
```
```{r}
# Function to find and average consecutive/near-consecutive samples
average_consecutive_samples <- function(data, window_days = 2) {
  data %>%
    arrange(date) %>%
    mutate(
      # Create groups for dates within window_days of each other
      date_diff = c(Inf, diff(date)),
      new_group = cumsum(date_diff > window_days)
    ) %>%
    group_by(new_group, group) %>%
    summarise(
      date = mean(date),  # Use mean date for the window
      contribution = mean(contribution, na.rm = TRUE),
      n_samples = n(),
      .groups = 'drop'
    ) %>%
    select(-new_group) %>%
    mutate(date = as.Date(date, origin = "1970-01-01"))
}

# Combine stations with consecutive day averaging
chem_c_tchla_combined <- chem_c_tchla %>%
  group_by(date, group) %>%
  summarise(
    contribution = mean(contribution, na.rm = TRUE),
    n_stations = n_distinct(station),
    .groups = 'drop'
  ) %>%
  arrange(group, date) %>%
  group_by(group) %>%
  mutate(
    date_diff = c(Inf, diff(date)),
    window_group = cumsum(date_diff > 2)  # Create new group when gap > 2 days
  ) %>%
  group_by(group, window_group) %>%
  summarise(
    date = mean(date),
    contribution = mean(contribution, na.rm = TRUE),
    n_samples = n(),
    .groups = 'drop'
  ) %>%
  select(-window_group) %>%
  mutate(date = as.Date(date, origin = "1970-01-01"))

# Define cutoff dates
cutoff_2020 <- as.Date("2020-02-11")
cutoff_2024 <- as.Date("2024-01-01")

# Split combined data into three periods
data_area_early <- chem_c_tchla_combined %>% filter(date < cutoff_2020)
data_bar_2020 <- chem_c_tchla_combined %>% filter(date == cutoff_2020)
data_area_mid <- chem_c_tchla_combined %>% filter(date > cutoff_2020 & date < cutoff_2024)
data_bars_late <- chem_c_tchla_combined %>% filter(date >= cutoff_2024)

# Create combined plot
ggplot() +
  # Area plot for pre-2020-02-11
  geom_area(data = data_area_early,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  # Single bar for 2020-02-11
  geom_bar(data = data_bar_2020,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  # Area plot for mid period
  geom_area(data = data_area_mid,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  # Bar plot for 2024-2025
  geom_bar(data = data_bars_late,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  scale_fill_npg_distinct() +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")")),
  ) +
  theme_bw() +
  theme(
    legend.position = "right",
    legend.key.height = unit(0.6, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    text = element_text(size = 28, color = "black")
  )

ggsave(here("figures", "phytoplankton_community_composition_combined_2day.png"), 
       width = 16, height = 6.5, dpi = 300)
```



```{r}
# Calculate monthly climatology from combined data
chem_climatology <- chem_c_tchla_combined %>%
  mutate(month = month(date)) %>%
  group_by(month, group) %>%
  summarise(
    contribution_mean = mean(contribution, na.rm = TRUE),
    contribution_sd = sd(contribution, na.rm = TRUE),
    n_years = n_distinct(year(date)),
    .groups = 'drop'
  ) %>%
  # Create a date for plotting (using 2020 as dummy year)
  mutate(date_plot = as.Date(paste("2020", month, "15", sep = "-")))

# Create climatology bar plot
ggplot(chem_climatology, aes(x = date_plot, y = contribution_mean, fill = fct_rev(group))) +
  geom_bar(position = "stack", stat = "identity", width = 25) +
  scale_fill_npg_distinct() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b",
               expand = c(0.02, 0)) +
  scale_y_continuous(expand = c(0, 0), limits = c(0, 6)) +
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")")),
    title = "Phytoplankton community composition - Monthly climatology",
    subtitle = paste("Based on", 
                     min(chem_climatology$n_years), "-", 
                     max(chem_climatology$n_years), "years of data per month")
  ) +
  theme_bw() +
  theme(
    legend.position = "right",
    legend.key.height = unit(0.6, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    text = element_text(size = 28, color = "black")
  )

ggsave(here("figures", "phytoplankton_climatology_monthly.png"), 
       width = 16, height = 7, dpi = 300)
```

```{r}
# Calculate monthly anomalies with monthly aggregation
chem_anomalies_monthly <- chem_c_tchla_combined %>%
  mutate(
    year = year(date),
    month = month(date),
    year_month = floor_date(date, "month")
  ) %>%
  # First aggregate to monthly means
  group_by(year_month, month, group) %>%
  summarise(
    contribution_monthly = mean(contribution, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  # Join with climatology
  left_join(
    chem_climatology %>% select(month, group, contribution_mean),
    by = c("month", "group")
  ) %>%
  # Calculate anomaly and classify as positive/negative
  mutate(
    anomaly = contribution_monthly - contribution_mean,
    anomaly_type = if_else(anomaly >= 0, "Positive", "Negative")
  )

# Plot faceted anomalies
ggplot(chem_anomalies_monthly, aes(x = year_month, y = anomaly, fill = anomaly_type)) +
  geom_hline(yintercept = 0, color = "black", linewidth = 0.5) +
  geom_bar(stat = "identity", width = 25) +
  facet_wrap(~ group, ncol = 1, scales = "free_y", strip.position = "right") +
  scale_fill_manual(
    values = c("Positive" = "#D32F2F", "Negative" = "#1976D2"),
    guide = "none"
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(expand = c(0.02, 0), labels = scales::number_format(accuracy = 0.01)) +
  labs(
    y = expression(paste("Biomass anomaly (mg ", m^{-3}, ")")),
  ) +
  theme_bw() +
  theme(
    strip.background = element_blank(),
    strip.text.y = element_text(face = "bold", size = 28, color = "black", angle = 270),
    strip.placement = "outside",
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    panel.spacing = unit(2, "lines"),  # Add gap between facets
    axis.title.x = element_blank(),
    axis.title.y = element_text(size = 28, color = "black"),
    axis.text = element_text(size = 28, color = "black"),
  )

ggsave(here("figures", "phytoplankton_anomalies_faceted.png"), 
       width = 16, height = 16, dpi = 300)
```

```{r}
#==============================================================================
# CHEMTAX Community Clustering Analysis
#==============================================================================

library(vegan)
library(cluster)
library(factoextra)
library(ggdendro)
library(ggplot2)
library(patchwork)
library(tidyverse)
library(here)

#------------------------------------------------------------------------------
# 1. PREPARE DATA
#------------------------------------------------------------------------------

# Reshape CHEMTAX data to wide format
chem_wide <- chem_c_tchla_combined %>%
  select(date, group, contribution) %>%
  pivot_wider(names_from = group, values_from = contribution, values_fill = 0)

# Create matrix for clustering
chem_matrix <- chem_wide %>%
  select(-date) %>%
  as.matrix()
rownames(chem_matrix) <- chem_wide$date

# Calculate relative composition
chem_rel <- chem_matrix / rowSums(chem_matrix)

#------------------------------------------------------------------------------
# 2. COLOR SCHEMES
#------------------------------------------------------------------------------

# NPG color palette for taxa
npg_distinct_colors <- c(
  "#E64B35",  # Red - diat
  "#4DBBD5",  # Light blue - raph
  "#00A087",  # Teal - dict
  "#3C5488",  # Dark blue - dino
  "#F39B7F",  # Orange - cryp
  "#8491B4",  # Purple - GA
  "#91D1C2",  # Light teal - hapt
  "#FFC107"   # Golden yellow - cyan
)

taxon_order <- c("diat", "raph", "dict", "dino", "cryp", "GA", "hapt", "cyan")
taxon_colors <- setNames(npg_distinct_colors[1:length(taxon_order)], taxon_order)

# Season colors - organized and visually distinct
season_colors <- c(
  "Winter" = "#3B4CC0",  # Deep blue
  "Spring" = "#7FBC41",  # Fresh green
  "Summer" = "#F46D43",  # Warm orange
  "Fall" = "#9E6F3E"     # Earthy brown
)

# Season order
season_order <- c("Winter", "Spring", "Summer", "Fall")

#------------------------------------------------------------------------------
# 3. CLUSTERING FUNCTION
#------------------------------------------------------------------------------

perform_clustering <- function(data_matrix, method = "relative") {
  
  # Always use Bray-Curtis (both datasets are compositional)
  dist_mat <- vegdist(data_matrix, method = "bray")
  
  # Use average linkage for both
  linkage <- "average"
  
  hc <- hclust(dist_mat, method = linkage)
  
  coph <- cophenetic(hc)
  cat("\n", method, "- Bray-Curtis +", linkage, 
      "| Cophenetic cor:", round(cor(dist_mat, coph), 3), "\n")
  
  return(list(hc = hc, dist = dist_mat, linkage = linkage))
}

#------------------------------------------------------------------------------
# 4. PLOTTING FUNCTION
#------------------------------------------------------------------------------

create_cluster_figure <- function(data_matrix, hc_obj, k, method = "relative",
                                  save_name) {
  
  # Cut tree
  clusters <- cutree(hc_obj, k = k)
  
  # Create clustered dataframe
  chem_clustered <- chem_wide %>%
    mutate(
      community_cluster = factor(clusters),
      total_biomass = rowSums(chem_matrix),
      season = case_when(
        format(date, "%m-%d") >= "03-20" & format(date, "%m-%d") <= "06-20" ~ "Spring",
        format(date, "%m-%d") >= "06-21" & format(date, "%m-%d") <= "09-21" ~ "Summer",
        format(date, "%m-%d") >= "09-22" & format(date, "%m-%d") <= "12-20" ~ "Fall",
        TRUE ~ "Winter"
      ),
      season = factor(season, levels = season_order)  # Order seasons
    )
  
  # Cluster sizes
  cluster_sizes <- chem_clustered %>%
    count(community_cluster) %>%
    mutate(label = paste0("n = ", n))
  
  # Cluster profiles
  cluster_profiles <- chem_clustered %>%
    pivot_longer(cols = -c(date, community_cluster, total_biomass, season),
                 names_to = "taxon", values_to = "biomass") %>%
    group_by(community_cluster, taxon) %>%
    summarise(
      mean_biomass = mean(biomass),
      mean_proportion = mean(biomass / (total_biomass + 0.001)),
      .groups = "drop"
    )
  
  #--- Panel A: Community Composition ---
  if (method == "relative") {
    # Stacked bar showing proportions
    p_comp <- cluster_profiles %>%
      mutate(taxon = factor(taxon, levels = taxon_order)) %>%
      ggplot(aes(x = community_cluster, y = mean_proportion, fill = taxon)) +
      geom_col(width = 0.7) +
      geom_text(data = cluster_sizes,
                aes(x = community_cluster, y = 1.05, label = label, fill = NULL),
                size = 5.5, fontface = "bold", vjust = 0) +
      scale_fill_manual(values = taxon_colors, name = "Taxon") +
      scale_y_continuous(labels = scales::percent, 
                        expand = expansion(mult = c(0, 0.1))) +
      labs(
        title = "Mean Community Composition by Cluster",
        subtitle = "Bray-Curtis distance, average linkage on relative abundance",
        x = "Community Cluster",
        y = "Mean Proportion of Community"
      )
  } else {  # absolute
    # Stacked bar showing absolute biomass
    p_comp <- cluster_profiles %>%
      mutate(taxon = factor(taxon, levels = taxon_order)) %>%
      ggplot(aes(x = community_cluster, y = mean_biomass, fill = taxon)) +
      geom_col(width = 0.7) +
      geom_text(data = cluster_sizes,
                aes(x = community_cluster, 
                    y = max(cluster_profiles$mean_biomass) * 1.05, 
                    label = label, fill = NULL),
                size = 5.5, fontface = "bold", vjust = 0) +
      scale_fill_manual(values = taxon_colors, name = "Taxon") +
      scale_y_continuous(expand = expansion(mult = c(0, 0.1))) +
      labs(
        title = "Mean Community Composition by Cluster",
        subtitle = "Bray-Curtis distance, average linkage on absolute biomass",
        x = "Community Cluster",
        y = expression(bold("Mean TChla (mg m"^-3*")"))
      )
  }
  
  p_comp <- p_comp +
    theme_minimal(base_size = 16) +
    theme(
      axis.text.x = element_text(size = 14, face = "bold"),
      axis.title.x = element_text(size = 16, face = "bold"),
      axis.title.y = element_text(size = 16),
      plot.title = element_text(size = 18, face = "bold", hjust = 0.5),
      plot.subtitle = element_text(size = 14, hjust = 0.5),
      legend.position = "right",
      legend.text = element_text(size = 14),
      legend.title = element_text(size = 15, face = "bold"),
      panel.grid.major.x = element_blank()
    )
  
  #--- Panel B: Total Biomass ---
  p_biomass <- chem_clustered %>%
    ggplot(aes(x = community_cluster, y = total_biomass, fill = community_cluster)) +
    geom_boxplot(outlier.size = 1.5) +
    scale_y_log10() +
    scale_fill_manual(values = npg_distinct_colors[1:k]) +
    labs(
      title = "Total Chlorophyll-a by Cluster",
      x = "Community Cluster",
      y = expression(bold("TChla (mg m"^-3*", log scale)"))
    ) +
    theme_minimal(base_size = 16) +
    theme(
      legend.position = "none",
      axis.text = element_text(size = 14),
      axis.title.x = element_text(size = 16, face = "bold"),
      axis.title.y = element_text(size = 16),
      plot.title = element_text(size = 18, face = "bold", hjust = 0.5),
      panel.grid.major.x = element_blank()
    )
  
  #--- Panel C: Seasonality ---
  p_season <- chem_clustered %>%
    count(community_cluster, season) %>%
    group_by(community_cluster) %>%
    mutate(prop = n / sum(n)) %>%
    ggplot(aes(x = community_cluster, y = prop, fill = season)) +
    geom_col(position = "stack", width = 0.7) +
    geom_text(aes(label = paste0(round(prop * 100), "%")),
              position = position_stack(vjust = 0.5),
              size = 5, fontface = "bold", color = "white") +
    scale_fill_manual(values = season_colors, 
                     breaks = season_order) +  # Ensures legend order
    scale_y_continuous(labels = scales::percent, 
                      expand = expansion(mult = c(0, 0.05))) +
    labs(
      title = "Seasonal Distribution by Cluster",
      x = "Community Cluster",
      y = "Proportion of Samples",
      fill = "Season"
    ) +
    theme_minimal(base_size = 16) +
    theme(
      axis.text = element_text(size = 14),
      axis.title = element_text(size = 16, face = "bold"),
      plot.title = element_text(size = 18, face = "bold", hjust = 0.5),
      legend.position = "right",
      legend.text = element_text(size = 14),
      legend.title = element_text(size = 15, face = "bold"),
      panel.grid.major.x = element_blank()
    )
  
  #--- Panel D: Dendrogram ---
  dend_data <- dendro_data(hc_obj)
  
  dend_data$labels <- dend_data$labels %>%
    mutate(cluster = clusters[match(label, names(clusters))])
  
  terminal_segs <- dend_data$segments %>%
    filter(yend == 0) %>%
    left_join(dend_data$labels %>% select(x, cluster), by = "x")
  
  p_dend <- ggplot() +
    geom_segment(data = dend_data$segments,
                 aes(x = x, y = y, xend = xend, yend = yend),
                 color = "gray50", linewidth = 0.5) +
    geom_segment(data = terminal_segs,
                 aes(x = x, y = y, xend = xend, yend = yend, 
                     color = factor(cluster)),
                 linewidth = 1.2) +
    scale_color_manual(values = npg_distinct_colors[1:k]) +
    labs(x = NULL, y = "Bray-Curtis Distance") +
    theme_minimal(base_size = 16) +
    theme(
      axis.text.x = element_blank(),
      axis.ticks.x = element_blank(),
      axis.text.y = element_text(size = 14),
      axis.title.y = element_text(size = 16, face = "bold"),
      panel.grid.major.x = element_blank(),
      legend.position = "none"
    )
  
  #--- Combine panels ---
  p_final <- p_comp / (p_biomass | p_season) / p_dend +
    plot_layout(heights = c(1.5, 1, 1.2))
  
  print(p_final)
  ggsave(here("figures", save_name),
         plot = p_final, width = 16, height = 16, dpi = 300, bg = "white")
  
  return(list(plot = p_final, clusters = chem_clustered))
}

#------------------------------------------------------------------------------
# 5. RUN ANALYSES
#------------------------------------------------------------------------------

# RELATIVE ABUNDANCE CLUSTERING
cat("\n=== RELATIVE ABUNDANCE CLUSTERING ===")
rel_results <- perform_clustering(chem_rel, method = "relative")
rel_output <- create_cluster_figure(
  data_matrix = chem_rel,
  hc_obj = rel_results$hc,
  k = 4,
  method = "relative",
  save_name = "chemtax_clusters_relative_complete.png"
)

# ABSOLUTE BIOMASS CLUSTERING
cat("\n=== ABSOLUTE BIOMASS CLUSTERING ===")
abs_results <- perform_clustering(chem_matrix, method = "absolute")
abs_output <- create_cluster_figure(
  data_matrix = chem_matrix,
  hc_obj = abs_results$hc,
  k = 4,
  method = "absolute",
  save_name = "chemtax_clusters_absolute_complete.png"
)

#------------------------------------------------------------------------------
# 6. COMPARE CLUSTERING METHODS
#------------------------------------------------------------------------------

# Extract cluster assignments
rel_clusters <- rel_output$clusters %>% select(date, rel_clust = community_cluster)
abs_clusters <- abs_output$clusters %>% select(date, abs_clust = community_cluster)

comparison <- left_join(rel_clusters, abs_clusters, by = "date")

cat("\n=== CLUSTER COMPARISON ===\n")
print(table(comparison$rel_clust, comparison$abs_clust))
```

```{r}
# Get cluster assignments (use the filtered 2016-2023 data if you removed 2024-2025)
cluster_points <- abs_output$clusters %>%
  distinct(date, community_cluster) %>%
  filter(!is.na(community_cluster))


#------------------------------------------------------------------------------
# Updated color schemes for clusters (different from taxa but complementary)
#------------------------------------------------------------------------------

# Cluster colors - darker, more muted tones to contrast with taxa
cluster_colors <- c(
  "1" = "#B8860B",  # Dark goldenrod - Mixed
  "2" = "#2E8B57",  # Sea green - Diatoms
  "3" = "#8B4513",  # Saddle brown - Dinoflagellates
  "4" = "#4682B4"   # Steel blue - Winter
)

# Cluster labels with descriptions
cluster_labels <- c(
  "1" = "1 (Mixed)",
  "2" = "2 (Diatoms)",
  "3" = "3 (Dinoflagellates)",
  "4" = "4 (Winter)"
)

#------------------------------------------------------------------------------
# Create seasonal background shading data (Spring and Summer only)
#------------------------------------------------------------------------------

# Create continuous seasonal background for each year
years <- 2016:2025
seasonal_backgrounds <- map_df(years, function(year) {
  data.frame(
    season = c("Spring", "Summer"),
    year = year,
    start_date = as.Date(c(
      paste0(year, "-03-20"),       # Spring
      paste0(year, "-06-21")        # Summer
    )),
    end_date = as.Date(c(
      paste0(year, "-06-20"),       # Spring ends
      paste0(year, "-09-21")        # Summer ends
    ))
  )
}) %>%
  # Keep only dates within our plot range
  filter(start_date >= as.Date("2016-01-01") & end_date <= as.Date("2025-12-31")) %>%
  mutate(season = factor(season, levels = c("Spring", "Summer")))

#------------------------------------------------------------------------------
# Create plots
#------------------------------------------------------------------------------

# Create main plot with spring and summer shading only
p_main <- ggplot() +
  # Add seasonal background shading - Spring and Summer only
  geom_rect(data = seasonal_backgrounds,
            aes(xmin = start_date, xmax = end_date, ymin = 0, ymax = Inf, 
                fill = season),
            alpha = 0.15) +
  
  # Area plot for pre-2020-02-11
  geom_area(data = data_area_early,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  # Single bar for 2020-02-11
  geom_bar(data = data_bar_2020,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  # Area plot for mid period
  geom_area(data = data_area_mid,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  # Bar plot for 2024-2025
  geom_bar(data = data_bars_late,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  
  scale_fill_manual(
    values = c(taxon_colors, season_colors[c("Spring", "Summer")]),
    breaks = names(taxon_colors),
    name = NULL
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(expand = c(0, 0)) +  # Add this line - starts y-axis at zero
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")")),
  ) +
  theme_bw() +
  theme(
    legend.position = "right",
    legend.key.height = unit(0.6, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(5.5, 5.5, 0, 5.5)
  )

# Create cluster strip panel (narrower, thicker bars, single-row legend)
p_clusters <- ggplot(cluster_points, aes(x = date, y = 1, fill = community_cluster)) +
  geom_tile(height = 0.5, width = 25) +  # Narrower height, thicker bars
  scale_fill_manual(
    values = cluster_colors,
    labels = cluster_labels,
    name = "Community Cluster"
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(expand = c(0, 0)) +
  theme_bw() +
  theme(
    legend.position = "bottom",
    legend.direction = "horizontal",
    legend.box = "horizontal",
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(1.2, "cm"),
    legend.title = element_text(size = 24, face = "bold", hjust = 0.5),
    legend.text = element_text(size = 20),
    legend.margin = margin(t = 5, b = 5),
    panel.grid = element_blank(),
    axis.title = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(0, 5.5, 5.5, 5.5)
  ) +
  guides(fill = guide_legend(
    title.position = "top",
    title.hjust = 0.5,
    nrow = 1  # Force single row
  ))

# Combine with patchwork
library(patchwork)
p_combined <- p_main / p_clusters + 
  plot_layout(heights = c(12, 1))

print(p_combined)
ggsave(here("figures", "phytoplankton_community_composition_combined_clusters.png"), 
       width = 16, height = 7.5, dpi = 300)
```

```{r}
chem_export <- chem_wide %>% 
  left_join(abs_clusters)

write.csv(chem_export, here("outputs", "chem_clusters_2025-12-03.csv"))
```

```{r}
#------------------------------------------------------------------------------
# Prepare integrated chlorophyll with consecutive sample averaging
#------------------------------------------------------------------------------

# Get all dates with CHEMTAX data
chemtax_dates <- unique(c(
  data_area_early$date,
  data_bar_2020$date,
  data_area_mid$date,
  data_bars_late$date
))

# Filter integrated chlorophyll to only dates with CHEMTAX data, then average
vert_filtered <- vert %>%
  filter(date >= as.Date("2016-01-01") & date <= as.Date("2025-12-31")) %>%
  filter(date %in% chemtax_dates) %>%  # Only keep dates with CHEMTAX data
  arrange(date) %>%
  mutate(
    date_diff = c(Inf, diff(date)),
    window_group = cumsum(date_diff > 2)  # Create new group when gap > 2 days
  ) %>%
  group_by(window_group) %>%
  summarise(
    date = mean(date),
    int_chla = mean(int_chla, na.rm = TRUE),
    n_samples = n(),
    .groups = 'drop'
  ) %>%
  select(-window_group) %>%
  mutate(date = as.Date(date, origin = "1970-01-01"))

# Calculate scaling factor for secondary axis
max_biomass <- max(c(
  max(data_area_early$contribution, na.rm = TRUE),
  max(data_area_mid$contribution, na.rm = TRUE),
  max(data_bars_late$contribution, na.rm = TRUE)
))

max_chla <- max(vert_filtered$int_chla, na.rm = TRUE)
scale_factor <- max_biomass / max_chla

#------------------------------------------------------------------------------
# Create main plot
#------------------------------------------------------------------------------

p_main <- ggplot() +
  # Seasonal background shading
  geom_rect(data = seasonal_backgrounds,
            aes(xmin = start_date, xmax = end_date, ymin = 0, ymax = Inf, 
                fill = season),
            alpha = 0.15) +
  
  # Area plot for pre-2020-02-11
  geom_area(data = data_area_early,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  
  # Single bar for 2020-02-11
  geom_bar(data = data_bar_2020,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  
  # Area plot for mid period
  geom_area(data = data_area_mid,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  
  # Bar plot for 2024-2025
  geom_bar(data = data_bars_late,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  
  # Add integrated chlorophyll - line only, subtle
  # geom_line(data = vert_filtered,
  #           aes(x = date, y = int_chla * scale_factor),
  #           color = "gray40", linewidth = 0.7) +
  
 # Add integrated chlorophyll line and points
  geom_line(data = vert_filtered,
            aes(x = date, y = int_chla * scale_factor),
            color = "black", linewidth = 0.5) +
  
  geom_point(data = vert_filtered,
             aes(x = date, y = int_chla * scale_factor),
             shape = 21, size = 2, fill = "white", color = "black", stroke = 1) +
  
  scale_fill_manual(
    values = c(taxon_colors, season_colors[c("Spring", "Summer")]),
    breaks = names(taxon_colors),
    name = NULL
  ) +
  
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  
  scale_y_continuous(
    expand = c(0, 0),
    sec.axis = sec_axis(~ . / scale_factor, 
                       name = expression(paste("Integrated Chl ", italic("a"), " (mg ", m^{-2}, ")")))
  ) +
  
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")"))
  ) +
  
  theme_bw() +
  theme(
    legend.position = "bottom",
    legend.direction = "horizontal",
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    text = element_text(size = 28, color = "black"),
    axis.title.y.right = element_text(margin = margin(l = 10)),
    plot.margin = margin(5.5, 5.5, 0, 5.5)
  ) +
  guides(fill = guide_legend(nrow = 1))

#------------------------------------------------------------------------------
# Create cluster strip panel
#------------------------------------------------------------------------------

p_clusters <- ggplot(cluster_points, aes(x = date, y = 1, fill = community_cluster)) +
  geom_tile(height = 0.5, width = 25) +
  
  scale_fill_manual(
    values = cluster_colors,
    labels = cluster_labels,
    name = "Community Cluster"
  ) +
  
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  
  scale_y_continuous(expand = c(0, 0)) +
  
  theme_bw() +
  theme(
    legend.position = "bottom",
    legend.direction = "horizontal",
    legend.box = "horizontal",
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(1.2, "cm"),
    legend.title = element_text(size = 24, face = "bold", hjust = 0.5),
    legend.text = element_text(size = 20),
    legend.margin = margin(t = 5, b = 5),
    panel.grid = element_blank(),
    axis.title = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(0, 5.5, 5.5, 5.5)
  ) +
  guides(fill = guide_legend(
    title.position = "top",
    title.hjust = 0.5,
    nrow = 1
  ))

#------------------------------------------------------------------------------
# Combine plots
#------------------------------------------------------------------------------

library(patchwork)
p_combined <- p_main / p_clusters + 
  plot_layout(heights = c(12, 1))

print(p_combined)
ggsave(here("figures", "phytoplankton_community_composition_combined_clusters_integrated.png"), 
       width = 16, height = 8.5, dpi = 300)
```

```{r}
#------------------------------------------------------------------------------
# Prepare CTD fluorescence data and calculate integrated chlorophyll
#------------------------------------------------------------------------------

# Get all dates with CHEMTAX data first
chemtax_dates <- unique(c(
  data_area_early$date,
  data_bar_2020$date,
  data_area_mid$date,
  data_bars_late$date
))

# Filter and average CTD data by date and pres (averaging across stations)
# Only keep dates that match CHEMTAX dates
ctd_fl_filtered <- ctd_fl %>%
  filter(date >= as.Date("2016-01-01") & date <= as.Date("2025-12-31")) %>%
  filter(date %in% chemtax_dates) %>%
  filter(!is.na(f_final)) %>%
  filter(!is.na(pres)) %>%
  mutate(
    pres = as.numeric(pres),
    f_final = as.numeric(f_final)
  ) %>%
  filter(pres <= 30) %>%
  # Average across stations for each date and pres
  group_by(date, pres) %>%
  summarise(
    f_final = mean(f_final, na.rm = TRUE),
    .groups = 'drop'
  )

# Calculate integrated chlorophyll directly from filtered CTD data (0-30m)
vert_filtered <- ctd_fl_filtered %>%
  arrange(date, pres) %>%
  group_by(date) %>%
  summarise(
    int_chla = pracma::trapz(pres, f_final),
    .groups = 'drop'
  ) %>%
  # Apply consecutive day averaging
  arrange(date) %>%
  mutate(
    date_diff = c(Inf, diff(date)),
    window_group = cumsum(date_diff > 2)
  ) %>%
  group_by(window_group) %>%
  summarise(
    date = mean(date),
    int_chla = mean(int_chla, na.rm = TRUE),
    n_samples = n(),
    .groups = 'drop'
  ) %>%
  select(-window_group) %>%
  mutate(date = as.Date(date, origin = "1970-01-01"))

#------------------------------------------------------------------------------
# Create CTD fluorescence section plot
#------------------------------------------------------------------------------

# Define color palette for fluorescence
colors_v3 <- c("#08519c", "#3182bd", "#6baed6", "#9ecae1", 
               "#c6dbef", "#ffffcc", "#ffeda0", "#fed976", 
               "#feb24c", "#fd8d3c", "#fc4e2a", "#e31a1c", "#bd0026")

p_ctd <- ggplot(ctd_fl_filtered, aes(x = date, y = pres, fill = f_final)) +
  geom_tile() +
  geom_hline(yintercept = 5, size = 1) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_reverse(
    expand = c(0, 0),
    breaks = seq(0, 30, by = 10)
  ) +
  scale_fill_gradientn(
    colors = colors_v3,
    values = scales::rescale(c(0, 2, 5, 10, 20, 30)),
    na.value = "grey90",
    name = "CTD Fluorescence"
  ) +
  labs(
    y = "Depth (m)"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.width = unit(2, "cm"),
    legend.key.height = unit(0.4, "cm"),
    legend.title = element_text(size = 24),
    legend.text = element_text(size = 20),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(5.5, 5.5, 0, 5.5)
  )

# Calculate scaling factor for secondary axis
max_biomass <- max(c(
  max(data_area_early$contribution, na.rm = TRUE),
  max(data_area_mid$contribution, na.rm = TRUE),
  max(data_bars_late$contribution, na.rm = TRUE)
))

max_chla <- max(vert_filtered$int_chla, na.rm = TRUE)
scale_factor <- max_biomass / max_chla

#------------------------------------------------------------------------------
# Create main CHEMTAX plot
#------------------------------------------------------------------------------

p_main <- ggplot() +
  geom_rect(data = seasonal_backgrounds,
            aes(xmin = start_date, xmax = end_date, ymin = 0, ymax = Inf, 
                fill = season),
            alpha = 0.15) +
  geom_area(data = data_area_early,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  geom_bar(data = data_bar_2020,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  geom_area(data = data_area_mid,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  geom_bar(data = data_bars_late,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  geom_line(data = vert_filtered,
            aes(x = date, y = int_chla * scale_factor),
            color = "black", linewidth = 0.5) +
  geom_point(data = vert_filtered,
             aes(x = date, y = int_chla * scale_factor),
             shape = 21, size = 2, fill = "white", color = "black", stroke = 1) +
  scale_fill_manual(
    values = c(taxon_colors, season_colors[c("Spring", "Summer")]),
    breaks = names(taxon_colors),
    name = "Phytoplankton Taxa"
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(
    expand = c(0, 0),
    sec.axis = sec_axis(~ . / scale_factor, 
                       name = expression(paste("Integrated Chl ", italic("a"), " (mg ", m^{-2}, ")")))
  ) +
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")"))
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_text(size = 24),
    legend.text = element_text(size = 20),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    text = element_text(size = 28, color = "black"),
    axis.title.y.right = element_text(margin = margin(l = 10)),
    plot.margin = margin(0, 5.5, 0, 5.5)
  ) +
  guides(fill = guide_legend(nrow = 1, title.position = "left"))

#------------------------------------------------------------------------------
# Create cluster strip panel
#------------------------------------------------------------------------------

p_clusters <- ggplot(cluster_points, aes(x = date, y = 1, fill = community_cluster)) +
  geom_tile(height = 0.5, width = 25) +
  scale_fill_manual(
    values = cluster_colors,
    labels = cluster_labels,
    name = "Community Cluster"
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(expand = c(0, 0)) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.box = "horizontal",
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(1.2, "cm"),
    legend.title = element_text(size = 24, hjust = 0),
    legend.text = element_text(size = 20),
    legend.margin = margin(t = 5, b = 5),
    panel.grid = element_blank(),
    axis.title = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(0, 5.5, 5.5, 5.5)
  ) +
  guides(fill = guide_legend(
    title.position = "left",
    nrow = 1
  ))

#------------------------------------------------------------------------------
# Combine all three panels
#------------------------------------------------------------------------------

library(patchwork)
p_combined <- p_ctd / p_main / p_clusters + 
  plot_layout(heights = c(4, 10, 1))

print(p_combined)
ggsave(here("figures", "phytoplankton_community_composition_combined_clusters_integrated.png"), 
       width = 16, height = 12, dpi = 300)
```

```{r}
#------------------------------------------------------------------------------
# Prepare CTD fluorescence data and calculate integrated chlorophyll
#------------------------------------------------------------------------------

# Get all dates with CHEMTAX data first
chemtax_dates <- unique(c(
  data_area_early$date,
  data_bar_2020$date,
  data_area_mid$date,
  data_bars_late$date
))

# Filter and average CTD data by date and pres (averaging across stations)
# Only keep dates that match CHEMTAX dates
ctd_fl_filtered <- ctd_fl %>%
  filter(date >= as.Date("2016-01-01") & date <= as.Date("2025-12-31")) %>%
  filter(date %in% chemtax_dates) %>%
  filter(!is.na(f_final)) %>%
  filter(!is.na(pres)) %>%
  mutate(
    pres = as.numeric(pres),
    f_final = as.numeric(f_final)
  ) %>%
  filter(pres <= 30) %>%
  # Average across stations for each date and pres
  group_by(date, pres) %>%
  summarise(
    f_final = mean(f_final, na.rm = TRUE),
    .groups = 'drop'
  )

# Calculate SCM depth (depth of maximum fluorescence for each date)
scm_depth <- ctd_fl_filtered %>%
  group_by(date) %>%
  slice_max(f_final, n = 1) %>%
  slice(1) %>%  # In case of ties, take the first
  ungroup()

# Calculate integrated chlorophyll directly from filtered CTD data (0-30m)
vert_filtered <- ctd_fl_filtered %>%
  arrange(date, pres) %>%
  group_by(date) %>%
  summarise(
    int_chla = pracma::trapz(pres, f_final),
    .groups = 'drop'
  ) %>%
  # Apply consecutive day averaging
  arrange(date) %>%
  mutate(
    date_diff = c(Inf, diff(date)),
    window_group = cumsum(date_diff > 2)
  ) %>%
  group_by(window_group) %>%
  summarise(
    date = mean(date),
    int_chla = mean(int_chla, na.rm = TRUE),
    n_samples = n(),
    .groups = 'drop'
  ) %>%
  select(-window_group) %>%
  mutate(date = as.Date(date, origin = "1970-01-01"))

#------------------------------------------------------------------------------
# Create CTD fluorescence section plot
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
# Create CTD fluorescence section plot
#------------------------------------------------------------------------------

# Define improved blue-yellow-red color palette with better mid-range contrast
colors_fluor <- c("#08519c", "#3182bd", "#6baed6", "#9ecae1", 
                  "#c6dbef", "#fef0d9", "#fdcc8a", "#fc8d59", 
                  "#e34a33", "#b30000")

p_ctd <- ggplot(ctd_fl_filtered, aes(x = date, y = pres, fill = f_final)) +
  geom_tile() +
  geom_hline(yintercept = 5, size = 1) +
  geom_point(data = scm_depth, 
             aes(x = date, y = pres),
             shape = 21, size = 2, fill = "white", color = "black", 
             stroke = 0.8, inherit.aes = FALSE) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_reverse(
    expand = c(0, 0),
    breaks = seq(0, 30, by = 10)
  ) +
  scale_fill_gradientn(
    colors = colors_v3,
    values = scales::rescale(c(0, 2, 5, 10, 20, 30)),
    na.value = "grey90",
    name = "CTD Fluorescence"
  ) +
  labs(
    y = "Depth (m)"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.width = unit(2, "cm"),
    legend.key.height = unit(0.4, "cm"),
    legend.title = element_text(size = 24),
    legend.text = element_text(size = 20),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(5.5, 5.5, 0, 5.5)
  )

# Calculate scaling factor for secondary axis
max_biomass <- max(c(
  max(data_area_early$contribution, na.rm = TRUE),
  max(data_area_mid$contribution, na.rm = TRUE),
  max(data_bars_late$contribution, na.rm = TRUE)
))

max_chla <- max(vert_filtered$int_chla, na.rm = TRUE)
scale_factor <- max_biomass / max_chla

#------------------------------------------------------------------------------
# Create main CHEMTAX plot
#------------------------------------------------------------------------------

p_main <- ggplot() +
  geom_rect(data = seasonal_backgrounds,
            aes(xmin = start_date, xmax = end_date, ymin = 0, ymax = Inf, 
                fill = season),
            alpha = 0.15) +
  geom_area(data = data_area_early,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  geom_bar(data = data_bar_2020,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  geom_area(data = data_area_mid,
            aes(x = date, y = contribution, fill = fct_rev(group)),
            position = "stack", alpha = 0.9) +
  geom_bar(data = data_bars_late,
           aes(x = date, y = contribution, fill = fct_rev(group)),
           position = "stack", stat = "identity", width = 20) +
  geom_line(data = vert_filtered,
            aes(x = date, y = int_chla * scale_factor),
            color = "black", linewidth = 0.5) +
  geom_point(data = vert_filtered,
             aes(x = date, y = int_chla * scale_factor),
             shape = 21, size = 2, fill = "white", color = "black", stroke = 1) +
  scale_fill_manual(
    values = c(taxon_colors, season_colors[c("Spring", "Summer")]),
    breaks = names(taxon_colors),
    name = NULL
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(
    expand = c(0, 0),
    sec.axis = sec_axis(~ . / scale_factor, 
                       name = expression(paste("Integrated Chl ", italic("a"), " (mg ", m^{-2}, ")")))
  ) +
  labs(
    y = expression(paste("Phytoplankton biomass (mg ", m^{-3}, ")"))
  ) +
  theme_bw() +
  theme(
    legend.position = c(0.02, 0.98),
    legend.justification = c(0, 1),
    legend.direction = "horizontal",
    legend.background = element_rect(fill = "white", color = "black", linewidth = 0.5),
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_blank(),
    legend.text = element_text(size = 20),
    legend.box.margin = margin(4, 4, 4, 4),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    text = element_text(size = 28, color = "black"),
    axis.title.y.right = element_text(margin = margin(l = 10)),
    plot.margin = margin(0, 5.5, 0, 5.5)
  ) +
  guides(fill = guide_legend(nrow = 1))

#------------------------------------------------------------------------------
# Create cluster strip panel
#------------------------------------------------------------------------------

p_clusters <- ggplot(cluster_points, aes(x = date, y = 1, fill = community_cluster)) +
  geom_tile(height = 0.5, width = 25) +
  scale_fill_manual(
    values = cluster_colors,
    labels = cluster_labels,
    name = "Community Cluster"
  ) +
  scale_x_date(limits = as.Date(c("2016-01-01", "2025-12-31")),
               expand = c(0, 0),
               date_breaks = "1 year", date_labels = "%Y") +
  scale_y_continuous(expand = c(0, 0)) +
  theme_bw() +
  theme(
    legend.position = "bottom",
    legend.direction = "horizontal",
    legend.box = "horizontal",
    legend.key.height = unit(0.5, "cm"),
    legend.key.width = unit(1.2, "cm"),
    legend.title = element_text(size = 24, hjust = 0),
    legend.text = element_text(size = 20),
    legend.margin = margin(t = 5, b = 5),
    panel.grid = element_blank(),
    axis.title = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank(),
    text = element_text(size = 28, color = "black"),
    plot.margin = margin(0, 5.5, 5.5, 5.5)
  ) +
  guides(fill = guide_legend(
    title.position = "left",
    nrow = 1
  ))

#------------------------------------------------------------------------------
# Combine all three panels
#------------------------------------------------------------------------------

library(patchwork)
p_combined <- p_ctd / p_main / p_clusters + 
  plot_layout(heights = c(4, 9, 1))

print(p_combined)
ggsave(here("figures", "phytoplankton_community_composition_combined_clusters_integrated2.png"), 
       width = 16, height = 11, dpi = 300)
```

```{r}
test <- chem_c_tchla_combined %>% 
  group_by(date) %>% 
  summarise(tchla = sum(contribution)) %>% 
  ungroup() %>% 
  distinct() %>% 
  left_join(vert_filtered) %>% 
  left_join(cluster_points)

# Add season and cluster columns to test
test <- test %>%
  mutate(
    season = case_when(
      format(date, "%m-%d") >= "03-20" & format(date, "%m-%d") <= "06-20" ~ "Spring",
      format(date, "%m-%d") >= "06-21" & format(date, "%m-%d") <= "09-21" ~ "Summer",
      format(date, "%m-%d") >= "09-22" & format(date, "%m-%d") <= "12-20" ~ "Fall",
      TRUE ~ "Winter"
    ),
    season = factor(season, levels = c("Winter", "Spring", "Summer", "Fall")),
    community_cluster = factor(community_cluster)
  )

# Create plot
test %>% 
  ggplot(aes(x = tchla, y = int_chla, 
             color = community_cluster, 
             shape = season)) +
  geom_point(size = 4, alpha = 0.8) +
  scale_color_manual(values = cluster_colors,
                     labels = cluster_labels,
                     name = "Community Cluster") +
  scale_shape_manual(values = c(16, 17, 15, 18),  # circle, triangle, square, diamond
                     name = "Season") +
  labs(x = "5m Total Chlorophyll-a (mg/m³)",
       y = "Integrated Chlorophyll-a (mg/m²)") +
  theme_bw(base_size = 16) +
  theme(
    legend.text = element_text(size = 14),
    legend.title = element_text(size = 15, face = "bold"),
    axis.title = element_text(size = 15, face = "bold"),
    axis.text = element_text(size = 13),
    panel.grid.minor = element_blank()
  )

ggsave(here("figures", "tchla_int_scatter.png"),
       width = 9,
       height = 6)
```




```{r}
#------------------------------------------------------------------------------
# Calculate stratification index for dates in the figure
#------------------------------------------------------------------------------

# Calculate stratification metric: max/integrated fluorescence
# Using only the dates that are in ctd_fl_filtered (which already matches chemtax_dates)
stratification_metrics <- ctd_fl_filtered %>%
  arrange(date, pres) %>%
  group_by(date) %>%
  summarise(
    f_max = max(f_final, na.rm = TRUE),
    f_integrated = pracma::trapz(pres, f_final),
    depth_range = max(pres) - min(pres),
    .groups = 'drop'
  ) %>%
  mutate(
    # Normalize by depth range to get average concentration
    f_mean_equiv = f_integrated / depth_range,
    # Stratification index: max / mean
    stratification_index = f_max / f_mean_equiv
  ) %>%
  mutate(
    # Categorize based on stratification index
    stratification_category = case_when(
      stratification_index < 1.5 ~ "Well-mixed",
      stratification_index >= 1.5 & stratification_index < 2.5 ~ "Weakly stratified",
      stratification_index >= 2.5 ~ "Strongly stratified"
    ),
    # Binary for filtering
    well_mixed = stratification_index < 1.5,
    stratified = stratification_index >= 1.5
  )

#------------------------------------------------------------------------------
# Calculate surface enhancement metric
#------------------------------------------------------------------------------

# Calculate SCM intensity: compares subsurface max to surface (0-5m) mean
scm_intensity <- ctd_fl_filtered %>%
  group_by(date) %>%
  summarise(
    f_surface = mean(f_final[pres <= 5], na.rm = TRUE),
    f_subsurface_max = max(f_final[pres > 5], na.rm = TRUE),
    depth_subsurface_max = pres[pres > 5][which.max(f_final[pres > 5])],
    .groups = 'drop'
  ) %>%
  mutate(
    # SCM intensity ratio
    scm_intensity = f_subsurface_max / f_surface,
    # Difference (for absolute scale)
    scm_enhancement_diff = f_subsurface_max - f_surface
  ) %>%
  mutate(
    # Categorize subsurface chlorophyll development
    scm_category = case_when(
      scm_intensity < 1.2 ~ "Surface dominated",
      scm_intensity >= 1.2 & scm_intensity < 1.5 ~ "Weak SCM",
      scm_intensity >= 1.5 ~ "Strong SCM"
    )
  )

# Rename stratification_metrics for clarity
vertical_concentration <- stratification_metrics %>%
  rename(
    vertical_concentration_index = stratification_index,
    concentration_category = stratification_category
  ) %>%
  mutate(
    concentration_category = case_when(
      concentration_category == "Well-mixed" ~ "Uniform",
      concentration_category == "Weakly stratified" ~ "Weakly concentrated",
      concentration_category == "Strongly stratified" ~ "Highly concentrated",
      TRUE ~ concentration_category
    )
  )



#------------------------------------------------------------------------------
# Create 4-panel stratification analysis figure
#------------------------------------------------------------------------------

# Panel 1: CTD fluorescence section
scm_depth_filtered <- scm_depth %>%
  left_join(vertical_concentration, by = "date") %>%
  filter(stratified == TRUE)  # Only show SCM when stratified

colors_v3 <- c("#08519c", "#3182bd", "#6baed6", "#9ecae1", 
               "#c6dbef", "#ffffcc", "#ffeda0", "#fed976", 
               "#feb24c", "#fd8d3c", "#fc4e2a", "#e31a1c", "#bd0026")

p1_ctd <- ggplot(ctd_fl_filtered, aes(x = date, y = pres, fill = f_final)) +
  geom_tile() +
  geom_hline(yintercept = 5, size = 1) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_reverse(
    expand = c(0, 0),
    breaks = seq(0, 30, by = 10)
  ) +
  scale_fill_gradientn(
    colors = colors_v3,
    values = scales::rescale(c(0, 2, 5, 10, 20, 30)),
    na.value = "grey90",
    name = "CTD Fluorescence"
  ) +
  labs(
    y = "Depth (m)",
    title = "(a) CTD Fluorescence Profiles"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.width = unit(1.5, "cm"),
    legend.key.height = unit(0.4, "cm"),
    legend.title = element_text(size = 18),
    legend.text = element_text(size = 14),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

# Panel 2: Integrated chlorophyll time series
p2_intchla <- ggplot(vert_filtered, aes(x = date, y = int_chla)) +
  geom_line(color = "black", linewidth = 0.8) +
  geom_point(shape = 21, size = 2.5, fill = "white", color = "black", stroke = 1) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_continuous(
    expand = expansion(mult = c(0.05, 0.1))
  ) +
  labs(
    y = expression(paste("Integrated Chl ", italic("a"), " (mg ", m^{-2}, ")")),
    title = "(b) Integrated Chlorophyll (0-30m)"
  ) +
  theme_bw() +
  theme(
    panel.grid.minor = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

# Panel 3: Vertical concentration index time series
p3_vertical_concentration <- ggplot(vertical_concentration, aes(x = date, y = vertical_concentration_index)) +
  geom_hline(yintercept = 1.5, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_hline(yintercept = 2.5, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_line(color = "black", linewidth = 0.8) +
  geom_point(aes(fill = concentration_category), 
             shape = 21, size = 2.5, color = "black", stroke = 0.8) +
  scale_fill_manual(
    values = c("Uniform" = "#d7191c", 
               "Weakly concentrated" = "#fdae61", 
               "Highly concentrated" = "#2b83ba"),
    name = "Vertical Distribution"
  ) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_continuous(
    expand = expansion(mult = c(0.05, 0.1))
  ) +
  labs(
    y = "Vertical Concentration Index\n(max / mean fluorescence)",
    title = "(c) Vertical Concentration Index"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.height = unit(0.4, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_text(size = 18),
    legend.text = element_text(size = 14),
    panel.grid.minor = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

# Panel 4: SCM intensity time series
p4_scm_intensity <- ggplot(scm_intensity, aes(x = date, y = scm_intensity)) +
  geom_hline(yintercept = 1.0, linetype = "solid", color = "gray70", linewidth = 0.5) +
  geom_hline(yintercept = 1.2, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_hline(yintercept = 1.5, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_line(color = "black", linewidth = 0.8) +
  geom_point(aes(fill = scm_category), 
             shape = 21, size = 2.5, color = "black", stroke = 0.8) +
  scale_fill_manual(
    values = c("Surface dominated" = "#fee0d2", 
               "Weak SCM" = "#fc9272", 
               "Strong SCM" = "#de2d26"),
    name = "SCM Development"
  ) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_continuous(
    expand = expansion(mult = c(0.05, 0.1))
  ) +
  labs(
    x = "Year",
    y = "SCM Intensity\n(subsurface max / surface mean)",
    title = "(d) SCM Intensity"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.height = unit(0.4, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_text(size = 18),
    legend.text = element_text(size = 14),
    panel.grid.minor = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

#------------------------------------------------------------------------------
# Combine all four panels
#------------------------------------------------------------------------------

# Create layout: 4 panels stacked vertically
p_combined_strat <- p1_ctd / p2_intchla / p3_vertical_concentration / p4_scm_intensity +
  plot_layout(heights = c(1, 1, 1, 1))

print(p_combined_strat)
ggsave(here("figures", "vertical_structure_analysis_4panel.png"), 
       width = 16, height = 16, dpi = 300)

#------------------------------------------------------------------------------
# Summary statistics
#------------------------------------------------------------------------------

cat("\nVertical Concentration Index Summary:\n")
print(summary(vertical_concentration$vertical_concentration_index))

cat("\nVertical Distribution Category Counts:\n")
print(table(vertical_concentration$concentration_category))

cat("\nSCM Intensity Summary:\n")
print(summary(scm_intensity$scm_intensity))

cat("\nSCM Category Counts:\n")
print(table(scm_intensity$scm_category))

cat("\nCorrelation between metrics:\n")
combined_metrics <- vertical_concentration %>%
  left_join(scm_intensity, by = "date")
cat("Vertical Concentration Index vs SCM Intensity: ", 
    cor(combined_metrics$vertical_concentration_index, 
        combined_metrics$scm_intensity, 
        use = "complete.obs"), "\n")

#------------------------------------------------------------------------------
# Export vertical structure metrics for RDA analysis
#------------------------------------------------------------------------------

# Combine both metrics and integrated chlorophyll into a single dataframe
vertical_metrics_export <- vertical_concentration %>%
  select(date, vertical_concentration_index, concentration_category, 
         well_mixed, stratified) %>%
  left_join(
    scm_intensity %>% select(date, scm_intensity, scm_category, 
                             f_surface, f_subsurface_max, depth_subsurface_max),
    by = "date"
  ) %>%
  left_join(
    vert_filtered %>% select(date, int_chla),
    by = "date"
  )

# Save as CSV
write.csv(vertical_metrics_export, 
          here("outputs", "vertical_structure_metrics.csv"),
          row.names = FALSE)



```

```{r}
library(patchwork)
library(here)
library(dplyr)
library(ggplot2)

# Calculate the comprehensive metrics
comprehensive_metrics <- ctd_fl_filtered %>%
  group_by(date) %>%
  summarise(
    # Basic metrics
    f_max = max(f_final, na.rm = TRUE),
    f_median = median(f_final, na.rm = TRUE),
    depth_max = pres[which.max(f_final)],
    depth_range = max(pres) - min(pres),
    
    # Integration
    f_integrated = pracma::trapz(pres, f_final),
    
    .groups = 'drop'
  ) %>%
  mutate(
    # Peakiness (robust concentration metric)
    peakiness = f_max / f_median,
    
    # Depth classification
    depth_category = case_when(
      depth_max <= 5 ~ "surface",
      depth_max > 5 & depth_max <= 20 ~ "subsurface",
      TRUE ~ "deep"
    ),
    
    # Peakiness categories for plotting
    peakiness_category = case_when(
      peakiness < 2 ~ "Well-mixed",
      peakiness >= 2 & peakiness < 3.5 ~ "Moderately peaked",
      peakiness >= 3.5 ~ "Highly peaked"
    )
  )

#------------------------------------------------------------------------------
# Panel 1: CTD fluorescence section
#------------------------------------------------------------------------------

colors_v3 <- c("#08519c", "#3182bd", "#6baed6", "#9ecae1", 
               "#c6dbef", "#ffffcc", "#ffeda0", "#fed976", 
               "#feb24c", "#fd8d3c", "#fc4e2a", "#e31a1c", "#bd0026")

p1_ctd <- ggplot(ctd_fl_filtered, aes(x = date, y = pres, fill = f_final)) +
  geom_tile() +
  geom_hline(yintercept = 5, size = 1, color = "black") +
  geom_hline(yintercept = 20, size = 1, linetype = "dashed", color = "black") +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_reverse(
    expand = c(0, 0),
    breaks = seq(0, 30, by = 10)
  ) +
  scale_fill_gradientn(
    colors = colors_v3,
    values = scales::rescale(c(0, 2, 5, 10, 20, 30)),
    na.value = "grey90",
    name = "CTD Fluorescence"
  ) +
  labs(
    y = "Depth (m)",
    title = "(a) CTD Fluorescence Profiles"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.width = unit(1.5, "cm"),
    legend.key.height = unit(0.4, "cm"),
    legend.title = element_text(size = 18),
    legend.text = element_text(size = 14),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

#------------------------------------------------------------------------------
# Panel 2: Integrated chlorophyll time series
#------------------------------------------------------------------------------

p2_intchla <- ggplot(comprehensive_metrics, aes(x = date, y = f_integrated)) +
  geom_line(color = "black", linewidth = 0.8) +
  geom_point(shape = 21, size = 2.5, fill = "white", color = "black", stroke = 1) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_continuous(
    expand = expansion(mult = c(0.05, 0.1))
  ) +
  labs(
    y = expression(paste("Integrated Chl ", italic("a"), " (mg ", m^{-2}, ")")),
    title = "(b) Integrated Chlorophyll (0-30m)"
  ) +
  theme_bw() +
  theme(
    panel.grid.minor = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

#------------------------------------------------------------------------------
# Panel 3: Peakiness time series
#------------------------------------------------------------------------------

p3_peakiness <- ggplot(comprehensive_metrics, aes(x = date, y = peakiness)) +
  geom_hline(yintercept = 2, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_hline(yintercept = 3.5, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_line(color = "black", linewidth = 0.8) +
  geom_point(aes(fill = peakiness_category), 
             shape = 21, size = 2.5, color = "black", stroke = 0.8) +
  scale_fill_manual(
    values = c("Well-mixed" = "#d7191c", 
               "Moderately peaked" = "#fdae61", 
               "Highly peaked" = "#2b83ba"),
    name = "Vertical Concentration"
  ) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_continuous(
    expand = expansion(mult = c(0.05, 0.1))
  ) +
  labs(
    y = "Peakiness\n(max / median fluorescence)",
    title = "(c) Peakiness Index"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.height = unit(0.4, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_text(size = 18),
    legend.text = element_text(size = 14),
    panel.grid.minor = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    axis.title.x = element_blank(),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

#------------------------------------------------------------------------------
# Panel 4: Depth of maximum fluorescence
#------------------------------------------------------------------------------

p4_depth_max <- ggplot(comprehensive_metrics, aes(x = date, y = depth_max)) +
  geom_hline(yintercept = 5, linetype = "solid", color = "gray70", linewidth = 0.8) +
  geom_hline(yintercept = 20, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  geom_line(color = "black", linewidth = 0.8) +
  geom_point(aes(fill = depth_category), 
             shape = 21, size = 2.5, color = "black", stroke = 0.8) +
  scale_fill_manual(
    values = c("surface" = "#fee0d2",
               "subsurface" = "#fc9272", 
               "deep" = "#2b83ba"),
    labels = c("surface" = "Surface (0-5m)",
               "subsurface" = "Subsurface (5-20m)",
               "deep" = "Deep (>20m)"),
    name = "Depth Category"
  ) +
  scale_x_date(
    limits = as.Date(c("2016-01-01", "2025-12-31")),
    expand = c(0, 0),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_reverse(
    limits = c(30, 0),
    breaks = seq(0, 30, by = 5),
    expand = expansion(mult = c(0.02, 0.02))
  ) +
  labs(
    x = "Year",
    y = "Depth of Maximum (m)",
    title = "(d) Depth of Fluorescence Maximum"
  ) +
  theme_bw() +
  theme(
    legend.position = "top",
    legend.direction = "horizontal",
    legend.key.height = unit(0.4, "cm"),
    legend.key.width = unit(0.8, "cm"),
    legend.title = element_text(size = 18),
    legend.text = element_text(size = 14),
    panel.grid.minor = element_blank(),
    text = element_text(size = 20, color = "black"),
    plot.title = element_text(hjust = 0, size = 20, face = "bold"),
    plot.margin = margin(5.5, 5.5, 5.5, 5.5)
  )

#------------------------------------------------------------------------------
# Combine all four panels
#------------------------------------------------------------------------------

p_combined_simple <- p1_ctd / p2_intchla / p3_peakiness / p4_depth_max +
  plot_layout(heights = c(1, 1, 1, 1))

print(p_combined_simple)

ggsave(here("figures", "vertical_structure_simple_4panel.png"), 
       width = 16, height = 16, dpi = 300)

# # Save the metrics for further analysis
# write_csv(comprehensive_metrics, here("data", "comprehensive_vertical_metrics.csv"))
```

```{r}
write_csv(comprehensive_metrics, here("outputs", "vert_fl_metrics_2026-01-14.csv"))
```






























